#!/usr/bin/env python3
import subprocess
import sys
from pathlib import Path

repo_root = str(Path(__file__).resolve().parent.parent)

result = subprocess.run(
    [sys.executable, "-m", "pytest", "tests/", "-v", "--rootdir", repo_root],
    cwd=repo_root,
)
sys.exit(result.returncode)
